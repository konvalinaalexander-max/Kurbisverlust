# Abmachungen mit dem Betrieb — und der Test, der jede beweist

Was der Betrieb festgelegt hat, muss die App fragen, speichern und verrechnen.
Diese Liste hält jede Absprache mit ihrer Kennung, ihrer Quelle und **dem Test
fest, der sie beweist**. Eine Zeile ohne grünen Test gilt als offen, egal wie
fertig der Code aussieht.

Die Tests laufen alle in `./supabase/test/run.sh` (Datenbank + Fachlogik) und
in den Prüfständen (`pruefstand/kette.mjs` für die Kette über die echten
Masken, `node pruefstand/luecken.mjs` für den Lückenscanner). Alles hängt in
der CI (`.github/workflows/pruefung.yml`).

| Kennung | Absprache | Quelle | Beweis |
|---|---|---|---|
| AB-01 | Beim Auftragsstart wird bestätigt, wie sortiert wird (Kiste ab x kg / Kaliber), vor Ort änderbar. Beide Arten dürfen je Sorte nebeneinander stehen. Nach Kaliber gibt es keine Überfüllung. | 1. Sept | `pruefung.sql` „AB-01 Sortierart geprüft"; `kette.mjs` (Art beim Eröffnen) |
| AB-02 | Der Palox wird bei Beginn und Abschluss abgelesen. Ohne Ablesung kein Abschluss. | 2. Sept | `pruefung.sql` „Palox …"; `kette.mjs` „ohne Ablesung kommt der Abschluss nicht an der Palox-Frage vorbei" — der Abschluss-Assistent beginnt mit der Ablesung |
| AB-03 | Der Ausschuss wird gewogen (Brutto, Kisten, Gebinde), Netto abgeleitet; Schätzung bleibt für Notfälle möglich. | 1. Sept | `pruefung.sql` „AB-03 Ausschuss geprüft"; `kette.mjs` (zu klein gewogen, zu gross geschätzt) |
| AB-04 | Was man vergessen kann, gehört an den Abschluss; der Abschluss erzwingt die Vollständigkeit. | 1. Sept | `kette.mjs` „Geführter Abschluss" (Assistent: Palox, Ausschuss, Charge, Zusammenfassung mit „Fehlt noch"); `pruefung.sql` (Angaben) |
| AB-05 | Zwei Ausschuss-Paletten-Fragen: „leer zu Beginn?" (Start) und „alles von dieser Arbeit?" (Abschluss). | 2. Sept | `kette_pruefen.sh` (beide Angaben kommen an); `kette.mjs` (Startfrage in der Checkliste, Abschlussfrage im Assistenten; eine vergessene Startfrage holt der Abschluss nach) |
| AB-06 | Die Chargennummer wird eingetippt, nicht aus einer Liste gewählt. | 1. Sept | `kette.mjs` (`#charge` im Assistenten, Schritt „Welche Charge?"); unbekannte Nummer blockiert „Weiter" |
| AB-07 | Erfassungsbeginn und Vorab-Ausgang je Charge; die Bilanz zählt den Vorlauf zum Ausgang. | 1. Sept | `pruefung.sql` „AB-07/08/09" (Vorlauf senkt Lücke um denselben Betrag) |
| AB-08 | Fax ist ein eigener Auftragstyp (fachlich ein Waschgang). | 2. Sept | `pruefung.sql` „AB-07/08/09" (ist_fax); `i18n.test.ts` (vierte Tätigkeit) |
| AB-09 | Die Lagerkontrolle hält fest, wie die Palette gegriffen wurde (zufällig erreichbar / Mitte-unten / gezielt). | 2. Sept | `pruefung.sql` „AB-07/08/09" (Auswahlart gehalten, Unsinn abgelehnt) |
| AB-10 | Hinweis in der Palox-Maske: Gewicht direkt von der Waage ablesen. | 2. Sept | im Frontend (`PaloxMaske`, `waageAblesenHinweis`); Bildschirm-Prüfstand `arbeit-palox` |
| AB-11 | Das Datum vom Palettenzettel ist Pflicht (Voraussetzung des Alters). | 1. Sept | `pruefung.sql` (Palette ohne Datum abgewiesen); `kette.mjs` (Zähler: „+" ohne Datum gesperrt; das Datum bleibt für die nächste Palette stehen) |
| AB-12 | Am Waschbecken werden Kisten gezählt; das Kistengewicht wird am Sortieren gemessen. | 3. Sept | `pruefung.sql` „Kisten am Waschbecken"; `v_koeff_gebinde` |
| AB-15 | Fax ist kein Waschgang: Etikettieren und Abpacken nach Bestellung. Erfasst werden die gemachten Kisten (je Kaliber, „+ 1 Palette") und das Faule, kistenweise gewogen (Brutto, Kistenart, mit/ohne Palette). Kein Palox, kein zu klein/zu gross, keine Kilo-Frage. Das Faule ist ein eigener Strom („Faul beim Abpacken"), nicht Teil der Verderbskurve. | 7. Sept | `pruefung.sql` Block 0051 (Netto per Auslöser, nicht gewaschen, eigener Strom, kein Modellpunkt); `kette.mjs` dritter Durchlauf + `kette_pruefen.sh` |
| AB-16 | Die Maschine sortiert immer nach Kaliber: Beim Eröffnen wird gefragt, *welche* Bänder heute eingestellt sind („wie zuletzt — übernehmen / anpassen"). Von Hand: Kiste ab x kg (welches x) oder Kaliber (welche Bänder). Eine Änderung ist eine neue, datierte Fassung; nichts wird überschrieben. | 7. Sept | `pruefung.sql` Block 0051 (`sortierschema_festlegen`: gleich → gleiche Fassung, anders → neue ab heute, lückenhaft → abgelehnt); `kette.mjs` (Bänder angepasst, Sollgewicht übernommen) |
| AB-13 | Import der Perigon-Warenausgangsdatei: immer die ganze Datei, die App erkennt neu / geändert / unverändert / verschwunden; Rohzeilen bleiben (Prüfsumme, Fingerabdruck); Lieferungen entstehen je Position und Charge; ab 1. Juli 2026, Journal L nicht, beide Firmen eine Halle, sechsstellige Nummern über `charge.perigon_nr`. Artikel werden bestätigt, nicht geraten. | 7. Sept | `test/warenausgang.test.ts`; `pruefung.sql` Blöcke 0050 und 0055 (einmal, zweimal, korrigiert, zurückgemeldet, nur der Betriebsleiter); Bildschirm-Prüfstand `betrieb-import` mit der Probedatei |
| AB-18 | Die Erfassung in der Halle ist punktuell — nicht jede Arbeit wird dokumentiert, vielleicht nur fünfzehn Waschgänge in einer Saison. Vollständig sind nur zwei Listen: der Wareneingang (Erntejournal) und der Warenausgang (Lieferscheine). Aus den Arbeiten kommen Raten, keine Mengen. Überblick und Ursachen behaupten deshalb keine Menge, die nur aus Arbeiten stammt („sortiert", „gewaschen", „wartet aufs Waschen"), und keine Vorhersage („was kostet Warten"). | 7. Sept | Überblick (Eingang, Ausgang gemessen; Verlust, Bestand Modell), Ursachen (vier Ursachen, je Sorte und Charge); `ENTSCHEIDUNGEN.md` Runde E |
| AB-19 | Am Waschbecken wählt der Vorarbeiter das Kaliber aus den Bändern der Sorte (Vorgabe §6 oder der Sortierlauf der Charge) — oder gibt ein eigenes Band ein, wenn das Etikett etwas anderes nennt. Das Kistengewicht zum eigenen Band ist nur bekannt, wenn beim Sortieren genau dieses Band gezählt wurde; sonst sagt es die Plausibilität. | 7. Sept | `pruefung.sql` Block 0054; `kette.mjs` vierter Durchlauf + `kette_pruefen.sh`; Bildschirme `neu-kaliber`, `neu-kaliber-eigen` |
| AB-17 | Es gibt kein FIFO. Eingang und Ausgang einer Charge verteilen sich über Wochen; der Bestand wird je Eingangstag geführt (Paletten des Tages minus gezählte Paletten mit diesem Zetteldatum) und die Kaskade rechnet je Eingangstag mit seinem Alter. „Liegt seit" ist eine Spanne. | 7. Sept | `pruefung.sql` Block 0051 (drei Kohorten, jüngste zuerst verarbeitet, Summe = Bestand, Spanne, `v_naechste_charge` je Kohorte); `v_charge_kohorte` |
| AB-20 | Eine Palette wird im Lager nicht schwerer. Wiegt sie beim Nachwiegen mehr als 1 % über dem Eingangsnetto, ist die Wägung ein Fehler (Tara, Kistenzahl, Zahlendreher): sie bleibt gespeichert, zählt nicht in die Verdunstungsrate und steht mit Grund unter Auffälligkeiten. Die Rate je Sorte ist nie negativ. | 7. Sept | `pruefung.sql` Block 0056; `run.sh` Stufe 4b (Aktualisierung mit schwereren Paletten); `v_verdunstung_messung.verwendbar`, `v_koeff_verdunstung`, `v_plausibilitaet` „Wägung" |
| AB-21 | Eine einzelne Zahl, die nicht darstellbar ist, macht eine Spalte unbekannt — nie einen ganzen Bildschirm leer. Jeder Koeffizient in der Hochrechnung ist ein Anteil (0 … 1), jede Masse nie negativ. Die Bilanz sagt im Klartext, wenn der Wareneingang fehlt, statt am Verhältnis Ausgang/Eingang abzubrechen. | 8. Sept | `pruefung.sql` Block 0058 (Helfer, Klammerung, alle vier Sichten mit `select *`); `zahl()`, `v_hochrechnung`, `verlust_ranking()`, `v_saisonbilanz`, `v_marge_buch` |
| AB-22 | Kein harter Zahlencast auf eine berechnete Grösse. Jeder der 93 Casts in den Auswertungssichten läuft durch `zahl()`; passt ein Wert nicht, ist er unbekannt statt ein Abbruch. Und die App lädt jede Sicht für sich: scheitert eine, fehlen nur ihre Zahlen, der Rest des Bildschirms steht — mit einer Liste, welche es waren. | 8. Sept | `pruefung.sql` Block 0059 (Zählprüfung über alle Sichten, dazu jede Dashboard-Sicht mit `select *`); `src/auswertung/daten.ts` sammelt `probleme`, `Probleme`-Karte auf allen vier Reitern |
| AB-23 | Die Halle wird punktuell erfasst; vollständig sind nur Eingang (Erntejournal) und Ausgang (Lieferscheine). Deshalb bestimmt **kein Zählen eine Menge**: „Ausgelagert" ist die Eingangsmasse, die hinter den verkauften Lieferungen steckt (geliefert ÷ verkaufsfähiger Anteil beim Alter am Liefertag, je Eingangstag nach Eingangsanteil), „noch im Haus" ist Eingang minus Ausgelagert je Eingangstag. Steckt hinter den Lieferungen mehr als je einlagert wurde, heisst das Überzählung und steht als Befund da. | 8. Sept | `pruefung.sql` Kaskade-Block (ohne Lieferung nichts ausgelagert; 4000 kg verkauft → 4000 < ausgelagert < Eingang; Lager + Ausgelagert = Eingang) und Block 0060 (Lieferung ohne Charge verteilt sich auf die Chargen der Sorte; je Charge Lager + Ausgelagert = Eingang + Überzählung; Lieferung an eine Charge ohne Eingang fällt auf); `v_lieferung_kohorte`, `mv_kaskade` |
| AB-24 | Fax kommt nach beiden Wegen und ist eine Fax-Art: gezählt wird die **Palettenzahl gesamt**, das Faule kistenweise gewogen, dazu freiwillig die Tage seit dem Waschen. Die Fax-Masse ist Paletten × gemessene Palettenmasse (je Sorte und Kistensystem, aus den gewogenen fertigen Paletten). | 8. Sept | `pruefung.sql` Block 0060 (Quelle `fax_paletten`, Masse = 3 × Palettenmasse, Beobachtung mit Paletten, Tagen, Kistensystem); `kette.mjs` dritter Durchlauf (`#paletten-plus`, `#ab-tage`) + `kette_pruefen.sh` (2 Paletten, 2 Tage, 544 kg) |
| AB-25 | Kein Käufer mehr. Gefragt wird das **Kistensystem** der Arbeit: Kiste ab x kg · x Stück je Kaliber · anderes. Nur die ersten beiden sind rechenbar: das Soll steht an der Arbeit (und schlägt die Fassung), die Stück-Erwartung kommt aus der Sortier-CSV. Fertige Paletten wiegen ist Information, bei Stück-Kisten keine Marge. | 8. Sept | `pruefung.sql` Block 0060 (Soll 8 → 0.5 kg Überfüllung je Kiste; Stück-Kiste: Erwartung = Stück × Bandmittel, keine Überfüllung) und Kette-Block (e) (Soll an der Arbeit schlägt die Fassung, ohne Kistensystem gilt sie wieder); `kette.mjs` (`#system-kiste_ab`, `#system-stueck`, Fassung ohne Käufer); `luecken.sh` (kaeufer als begründete Ausnahme) |
| AB-26 | Beim Waschen + Sortieren wird je gezählter Palette das **Gewicht vom Zettel** getippt (Pflicht, jede Palette neu); zu klein / zu gross wird nicht mehr gewogen — der Anteil kommt aus der Sortier-CSV. Das Netto folgt der Palette im Wareneingang (Charge, Datum, Brutto) oder, wenn keine passt, der mittleren Tara der Charge; ein Zettelgewicht ohne Palette fällt auf. | 8. Sept | `pruefung.sql` Block 0060 (950 → exakt, 930 → Charge-Tara, Plausibilität „Zettelgewicht"); `kette.mjs` („+" ohne Gewicht gesperrt, Gewicht ins Wiegen vorbelegt) + `kette_pruefen.sh` (950 an drei Paletten, Quelle `zettel`); `luecken.sh` (ausschuss_messung ohne Maske, begründet) |
| AB-27 | Beim Waschen trägt jede gezählte Kiste ihr **Sortierdatum** von der Kiste; „kein Datum" ist eine Antwort, kein Auslassen. Die Kisten je Kaliber werden über die Sortierdaten summiert. | 8. Sept | `pruefung.sql` Block 0060 (4 + 3 + 2 = 9; ein zweiter Zähler ohne Datum wird abgewiesen); `kette.mjs` vierter Durchlauf (`#kistendatum`, `#kein-datum`) + `kette_pruefen.sh` (3 datiert, 1 ohne) |
| AB-28 | Zwei Stationen: die **Waschstrasse** (Waschen, Waschen + Sortieren — ein Palox) und die **Sortiermaschine**. Ein gefallener Palox-Stand heisst geleert: kein Häkchen, die Menge dieser Arbeit ist dann unbekannt — nie negativ, nie eine erfundene Zahl. Beim Waschen ist die Ablesung freiwillig. | 8. Sept | `pruefung.sql` Palox-Block (Waschstrasse gemeinsam 165 → 195 = 30 kg; gefallen 195 → 90 ohne Menge, Befund „Palox geleert", keine Schimmelmenge; 90 → 130 = 40 kg); `kette.mjs` (`#palox-ohne` beim Waschen, Palox am Start beim Waschen + Sortieren) |
| AB-29 | Palette kontrollieren schlägt **drei Chargen** vor (bestandsstärkste mit den wenigsten Kontrollen), fragt Eingangsdatum und Eingangsgewicht vom Zettel und nimmt mehrere Paletten nacheinander — die Charge bleibt stehen. | 8. Sept | `pruefung.sql` Block 0060 (höchstens drei Vorschläge); `kette.mjs` (zweimal hintereinander auf einer anderen Charge) + `kette_pruefen.sh` (2 Kontrollen an 1613) |
| AB-30 | „Buch A" und „Buch B" kommen in der Oberfläche nicht vor. Die Ursachen trennen **echten Verlust** (Palox, Verdunstung) von **keinem echten Verlust** (zu klein, zu gross, Überfüllung); der Überblick zeigt den Verlust zusammen, nach Gesamt, Sorte, Schlag und Charge, das Faule aus dem Palox als „Palox". | 8. Sept | `test/oberflaeche.test.ts` (kein „Buch A/B" in Texten und Seiten); Bildschirm-Prüfstand `ueberblick`, `ueberblick-sorte`, `ursachen` |
| AB-31 | Alle Zahlen der Auswertung gelten **bis heute**, nicht bis zum Saisonende. Der Verlust ist der bis heute gealterte, „noch im Haus" ist Eingang − Ausgang − Verlust bis heute. Was danach käme, steht getrennt als Prognose (gestrichelt, ab der Heute-Marke) und wird nie in eine Kennzahl gemischt. | 9. Sept | `pruefung.sql` Block 0061 (`heute_test`, `verlust_heute_kg` = Verdunstung + Schimmel + Sockel + Fax; `erg_verlauf.prognose` erst ab heute); Bildschirm-Prüfstand `ueberblick` |
| AB-32 | Die App **rechnet nichts beim Anzeigen**. Sie liest gespeicherte Ergebnisse (`erg_*`) und rechnet nur auf Knopfdruck neu — in **fünf Schritten** mit Fortschritt, jeder für sich kurz genug für eine Verbindung. Schritt 1 bringt zuerst die Statistik der Rohtabellen auf Stand (sonst rechnet der Planer nach einem grossen Import mit alten Zahlen: gemessen 33 s statt 1.1 s). Ein Schritt, der scheitert, nennt sich beim Namen; die übrigen Zahlen bleiben stehen. | 9. Sept | `run.sh` Stufe 5 und 6 (jeder der fünf Schritte einzeln unter 6 s, Ansichtenliste aus dem Frontend gelesen, Stufe 4b rechnet direkt nach dem Import); `bildschirme.mjs` (Attrappe für `auswertung_schritt`) |
| AB-33 | Beim **Waschen** werden Paletten gezählt, nicht Kisten: je Palette aus dem Zwischenlager das **Sortierdatum vom Zettel** (oder ausdrücklich keines) und die **Kistenzahl** darauf. Die Masse ist Kisten × gemessenes Kistengewicht des Kalibers; die Zeit im Zwischenlager kommt massegewichtet aus den Sortierdaten. | 9. Sept | `pruefung.sql` Block 0061 (`v_auftrag_wasch_paletten`, Wasch-Paletten zählen nicht als Eingangspaletten); `kette.mjs` vierter Durchlauf (`#sortierdatum`, `#kisten-palette`, `#wasch-plus`) + `kette_pruefen.sh` (3 Paletten, 96 Kisten, 2 datiert) |
| AB-34 | Beim **Waschen + Sortieren** sind Eingangsdatum und Zettelgewicht je Palette Pflicht; die App erinnert daran, **mindestens drei Paletten zu wiegen, bevor sie in die Waschmaschine kommen** — abschliessen geht trotzdem. Am Ende wird zu klein / zu gross **Palette für Palette gewogen** (Gewicht, Kisten, Kistenart); „nichts zu klein oder zu gross" ist eine Messung mit 0 kg, kein Auslassen. | 9. Sept | `kette.mjs` erster Durchlauf (Erinnerung „1 von 3 gewogen", `#aus-eintragen` 60 kg brutto → 29 kg netto) + `kette_pruefen.sh` (`ausschuss_messung` gemessen, `v_ausschuss_beobachtung`); `test/oberflaeche.test.ts` (nur die Ausschuss-Maske schreibt, nur mit Brutto oder 0) |
| AB-35 | **Drei fertige Paletten** wiegen: beim Waschen verlangt (oder so viele, wie die Arbeit hergibt), beim Waschen + Sortieren erinnert. Erst daraus weiss die Auswertung, was eine Kiste wirklich wiegt. | 9. Sept | `kette.mjs` vierter Durchlauf (drei `ausgang_wiegung`, Schritt „3 fertige Paletten gewogen"); `kette_pruefen.sh` (3 Wägungen mit Kaliber und Stück je Kiste) |
| AB-36 | Die **Lagerkontrolle** fragt nicht mehr „davon faul" und nicht mehr „wie gegriffen". Die Palette wird gewogen, nicht ausgepackt — was faul ist, sieht dabei niemand, und die Auswahl kann niemand beurteilen. Vorgeschlagen wird, wo eine Wägung am meisten bringt: **Bestand heute × Tage seit der letzten Wägung**. | 9. Sept | `pruefung.sql` Block 0061 (`v_kontrolle_vorschlag.informationswert`); `kette.mjs` fünfter Durchlauf (kein `#k-faul`, kein `#k-auswahl`) + `kette_pruefen.sh` (`faul_kg is null`, kein Punkt der Schimmelkurve) |
| AB-37 | **Überfüllung** wird nicht mehr je Käufer gezeigt, sondern getrennt nach Kistensystem und nur aus **gemessenen** Grössen: „Kiste ab x kg" nennt Soll, gemessenes Gewicht je Kiste, den Überschuss und die **aus den Verkaufsdateien gezählten verkauften Kisten**; verschenkt wird nur beziffert, wo beides vorliegt. Bei „x Stück je Kiste" gibt es keine Marge, nur die Abweichung vom erwarteten Stückgewicht. | 9. Sept | `pruefung.sql` Block 0061 (`v_ueberfuellung_verkauf`, `v_marge_buch` ohne Datei NULL); Bildschirm-Prüfstand `ursachen` |
| AB-38 | Auffälligkeiten stehen **nur unter Messungen**, mit dem Weg zur Korrektur: „korrigieren" öffnet die Arbeit, wo der Betriebsleiter jede Messung dieser Arbeit ändern oder löschen kann. Gespeichert bleibt die Beobachtung; das Abgeleitete rechnet die Datenbank neu. | 9. Sept | Bildschirm-Prüfstand `arbeit-korrektur`, `messungen`; RLS aus 0002 (`ist_admin()` oder Korrekturfenster) |
| AB-39 | **Jede Zahl sagt, was sie ist.** Keine Masse steht auf einem Bildschirm ohne Beschriftung; jede Beschriftung einer Masse steht im Begriffslexikon `pruefstand/begriffe.json` mit Bedeutung und Herkunftsspalte. „Verlust" kommt nie ohne Zusatz vor (bis heute · echter · kein echter · Prognose). Jede gerechnete Grösse trägt in ihrer Karte die Herkunftsmarke, die zu ihr gehört; jede Prozentzahl nennt ihre Bezugsgrösse. Ein neuer Name muss ins Lexikon, sonst schlägt der Prüfstand an. | 10. Sept | `node pruefstand/beschriftung.mjs` — 12 Ansichten des Betriebsleiters im Browser, 2667 Zahlen geerntet, Regeln R1–R6 und die Gegenprobe der vier Kopfzahlen gegen `erg_bilanz` |
| AB-40 | Eine Lieferung mit **Datum in der Zukunft** zählt in keine Zahl, die „bis heute" heisst — sie erscheint von selbst, sobald ihr Tag da ist. Verschwiegen wird sie nicht: Sie steht als Befund („Lieferung in der Zukunft") mit Masse, Datum und Rat. | 10. Sept | `pruefung.sql` Block 0062 (Lieferung auf heute + 10 ändert `v_lieferung_kohorte` um 0 kg und erscheint in `v_plausibilitaet`) |
| AB-41 | Eine Lieferung **an die Tiere** oder in den Nebenkanal (Buch „marge") ist ein **Ausgang**: Sie hat den Betrieb verlassen und liegt nicht mehr im Lager. Die Kaskade nimmt beide Bücher — und summiert dabei über das Buch, damit keine Kohorte zweimal zählt. Die Bilanz Eingang = verkauft + Verlust bis heute + anderer Kanal + noch im Haus + Überzählung geht bis auf Rundung auf; was übrig bleibt, heisst Überzählung und ist ein Datenfehler, kein Verlust. | 10. Sept | `pruefung.sql` Block 0062 (400 kg Tierfutter kommen als 400 kg an, `bilanz_rest_kg` bleibt unter 1 kg) |
| AB-42 | Die **Lagerkontrolle ist korrigierbar.** Eine zwischendurch gewogene Palette gehört zu keiner Arbeit; sie stand darum in der Auswertung, war aber über keine Arbeit erreichbar. Unter Messungen → Gewogene Paletten → „Lagerkontrollen berichtigen" lässt sich jede dieser Wägungen ändern oder löschen, samt Chargennummer. | 10. Sept | `Kontrollkorrektur` in `src/arbeit/Korrektur.tsx` (Filter `auftrag_id is null`); Bildschirm-Prüfstand `messungen` |
| AB-43 | Wer `setup.sql` in den Supabase-Editor einfügt, sieht **keine Meldung ausser der Schlusszeile**. Die Datei räumt vor jedem Anlegen auf; auf einer leeren Datenbank gäbe das 135 Zeilen „does not exist, skipping", die aussehen wie Fehler und keine sind. Sie sind stummgeschaltet; Warnungen und Fehler kommen durch. Was der Nutzer wissen muss — Chargen, Sorten, ob gerechnet wurde, ob pg_cron da ist — steht in der einen Fertig-Zeile. | 11. Sept | `run.sh` Stufe 2 zählt die Hinweiszeilen und bricht ab, sobald es mehr als null sind |
| AB-44 | Der **Unterbau bleibt aktuell**: React, Router, Vite, TypeScript und Supabase stehen auf ihren heutigen Hauptversionen, und was beim Benutzer ankommt, hat keine gemeldete Schwachstelle. Kompilate dürfen nicht neben dem Quelltext liegen — Vite zieht `App.js` dem `App.tsx` vor, und dann baut jeder Build eine eingefrorene Kopie. | 11. Sept | `npm audit --omit=dev` ohne Befund; `npm run pruefen` (tsc, 76 Tests, Build); `.gitignore` fängt `src/**/*.js` ab |

| AB-45 | `setup.sql` **passt in den SQL-Editor und ergibt nachweislich dieselbe Datenbank** wie die Migrationen einzeln. Der Editor schickt die Datei als einen Anfragekörper und nimmt höchstens 1 MB; die blosse Aneinanderreihung war 1,14 MB und wurde mit „Query is too large to be run via the SQL Editor" abgewiesen. Die Datei wird deshalb verdichtet: Teil A die Geschichte von Tabellen und Daten, Teil B das Rechenwerk in seiner heutigen Fassung. | 9. Sept | `run.sh` Stufe 2 vergleicht die Fingerabdrücke beider Wege (2613 Objekte, kein Unterschied) und misst die Grösse gegen 1000 KB |
| AB-46 | **Jede Ansicht sagt, was sie ist, und keine Funktion gehört jedermann.** Eine Ansicht ohne Beschreibung ist im SQL-Editor und in jedem auslesenden Werkzeug eine Zahl ohne Bedeutung; 26 hatten keine mehr, weil `drop … cascade` die Beschreibung der mitgerissenen Ansichten still entfernt. Ausführungsrechte gehören auf `authenticated` und `anon`, nie auf PUBLIC. | 9. Sept | `pruefung.sql`, Abschnitt 0063: zählt Ansichten ohne Beschreibung und Funktionen mit PUBLIC-Recht, beides muss null sein |

Noch offen (in `FRAGEN.md`, keine App-Änderung ohne Antwort):

| Kennung | Absprache / Frage | Stand |
|---|---|---|
| AB-14 | Direkte Messung beim Leeren des Palox (wie viel nicht faul). | offen; entscheidet, ob der Sockel am Saisonende erkennbar wird (`STATISTIK_BEFUND.md`) |

## Wie diese Datei ehrlich bleibt

Der Lückenscanner (`./pruefstand/luecken.sh`, Teil von `run.sh`) prüft in
beide Richtungen, dass zwischen Maske und Auswertung nichts nur auf einer Seite
existiert:

- **Vorwärts:** Jede Spalte der Erfassungstabellen, die ein Arbeiter füllt,
  wird von der App geschrieben (oder steht mit Begründung auf der Ausnahmeliste
  im Scanner). Eine Spalte, die die Datenbank kennt und die App nie schreibt,
  ist genau die Lücke, die AB-02 und AB-07 waren.
- **Rückwärts:** Jede Erfassungstabelle, aus der die Auswertung liest, wird von
  der App auch geschrieben.

Wer eine neue Abmachung umsetzt, trägt sie hier ein und nennt den Test. Wer eine
Spalte anlegt, die niemand füllt, hört es beim nächsten `run.sh`.
